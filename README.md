# network-dos-detector
Real-time network traffic monitor for Windows that detects potential DoS activity by tracking per-IP packet rates using Scapy and Npcap. Detection-only — no firewall rules are modified.
